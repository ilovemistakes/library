{% extends 'base.html.twig' %}

{% block body %}
    <h1>Record</h1>

    <table>
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ record.id }}</td>
            </tr>
            <tr>
                <th>Action</th>
                <td>{{ record.action }}</td>
            </tr>
        </tbody>
    </table>

    <ul>
        <li>
            <a href="{{ path('record_index') }}">Back to the list</a>
        </li>
        <li>
            <a href="{{ path('record_edit', { 'id': record.id }) }}">Edit</a>
        </li>
        <li>
            {{ form_start(delete_form) }}
                <input type="submit" value="Delete">
            {{ form_end(delete_form) }}
        </li>
    </ul>
{% endblock %}
